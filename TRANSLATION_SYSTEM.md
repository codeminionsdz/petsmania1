# نظام الترجمات متعدد اللغات - Multi-Language Translation System

## 📋 ملخص النظام

تم إضافة نظام ترجمات بسيط وآمن يدعم:
- **العربية** (ar)
- **الفرنسية** (fr)  
- **الإنجليزية** (en)

## ✨ الميزات

✅ **بدون مكتبات خارجية** - استخدام Objects و Context بسيط فقط
✅ **لا تغيير على Business Logic** - التعديلات قاصرة على الـ UI فقط
✅ **تغيير اللغة بدون Reload** - يحفظ الاختيار في localStorage
✅ **دعم RTL كامل** - اللغة العربية تظهر من اليمين تلقائياً
✅ **أداء عالي** - بدون overhead إضافي

## 📁 بنية الملفات

```
lib/
  ├── translations.ts          # ملف الترجمات الرئيسي
  └── language-context.tsx     # Context لإدارة اللغة

hooks/
  └── use-translation.ts       # Hook للحصول على الترجمات

components/layout/
  ├── language-switcher.tsx    # مكون تبديل اللغات
  ├── header.tsx               # Header مع الترجمات
  └── footer.tsx               # Footer مع الترجمات

app/
  ├── layout.tsx               # Layout الرئيسي
  ├── login/page.tsx           # صفحة اللوجن المترجمة
  └── register/page.tsx        # صفحة التسجيل المترجمة
```

## 🎯 كيفية الاستخدام

### 1. استخدام useTranslation Hook في المكونات

```tsx
"use client"

import { useTranslation } from "@/hooks/use-translation"

export function MyComponent() {
  const { t } = useTranslation()

  return <h1>{t("home.featured")}</h1>
}
```

### 2. إضافة ترجمات جديدة

عدّل ملف `lib/translations.ts`:

```ts
export const translations = {
  en: {
    "my_key": "English text",
  },
  fr: {
    "my_key": "Texte français",
  },
  ar: {
    "my_key": "نص عربي",
  },
}
```

### 3. تبديل اللغة من اللوجن Context

```tsx
import { useLanguage } from "@/lib/language-context"

export function MyComponent() {
  const { language, setLanguage, isRTL } = useLanguage()

  return (
    <div>
      <button onClick={() => setLanguage("ar")}>
        العربية
      </button>
      <p>اللغة الحالية: {language}</p>
      <p>هل RTL؟ {isRTL ? "نعم" : "لا"}</p>
    </div>
  )
}
```

## 🌍 معاينة في المتصفح

1. **الصفحة الرئيسية**: `http://localhost:3000`
2. **صفحة اللوجن**: `http://localhost:3000/login`
3. **صفحة التسجيل**: `http://localhost:3000/register`

ستجد مكون Language Switcher في الـ Header يسمح بتبديل اللغات فوراً!

## 🛠️ التخزين المحلي

- يتم حفظ اللغة المختارة في `localStorage` باسم `"language"`
- عند إعادة تحميل الصفحة، يتم استعادة اللغة المحفوظة تلقائياً

## 📱 دعم RTL

عند اختيار اللغة العربية:
- يتم تعيين `dir="rtl"` على عنصر `<html>`
- جميع النصوص والعناصر تتجاه من اليمين تلقائياً
- الـ Flexbox والـ Grid يتكيفون مع RTL

## 🔍 قائمة الترجمات المتاحة

### Header Translations
- `search.placeholder` - بحث
- `header.track` - تتبع الطلب
- `header.support` - الدعم
- `header.free_shipping` - شحن مجاني
- `header.login` - تسجيل الدخول
- `header.register` - إنشاء حساب
- ... وغيرها

### Login Page Translations
- `login.title` - عنوان اللوجن
- `login.subtitle` - الوصف
- `login.email` - البريد الإلكتروني
- `login.password` - كلمة المرور
- `login.button` - زر اللوجن
- `login.error_*` - رسائل الأخطاء
- ... وغيرها

### Register Page Translations
- `register.title` - عنوان التسجيل
- `register.first_name` - الاسم الأول
- `register.last_name` - اسم العائلة
- `register.email` - البريد الإلكتروني
- `register.password` - كلمة المرور
- `register.error_*` - رسائل الأخطاء
- ... وغيرها

## ⚙️ الإعدادات التقنية

### Context المستخدم

```tsx
interface LanguageContextType {
  language: Language
  setLanguage: (lang: Language) => void
  isRTL: boolean
}
```

### CSS للـ RTL

تم إضافة CSS في `app/globals.css` لدعم:
- تغيير direction للـ HTML
- تعديل Padding/Margin حسب اللغة
- محاذاة النصوص الصحيحة
- دعم Input مع RTL

## 🚀 الخطوات التالية (اختيارية)

1. إضافة ترجمات إضافية لصفحات أخرى
2. إضافة اختيار اللغة في لوحة المستخدم
3. إضافة دعم للغات إضافية
4. تخزين تفضيل اللغة في قاعدة البيانات (بدلاً من localStorage)

## 📝 ملاحظات مهمة

- النظام **آمن تماماً** - لا يؤثر على Business Logic
- **بدون Reload** - التغيير فوري ومسلس
- **خفيف الوزن** - بدون مكتبات ثقيلة
- **متوافق** - يعمل مع Next.js 16 و React الحديثة
