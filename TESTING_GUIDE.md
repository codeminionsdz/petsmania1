# 🧪 Testing Checklist

## تأكد من تشغيل الـ Server:

```bash
npm run dev
```

---

## السيناريو: من الصفر

### الخطوة 1: Checkout (كضيف)
```
[ ] اذهب الصفحة الرئيسية
[ ] اختر منتج (مثل Bioderma)
[ ] أضفه للسلة
[ ] اضغط Checkout
[ ] ملّ البيانات:
    [ ] الاسم الأول: أحمد
    [ ] الاسم الأخير: علي
    [ ] الهاتف: 0555123456 ⭐️ (حفظ هذا الرقم)
    [ ] العنوان: شارع الاستقلال
[ ] أتمم الطلبية
[ ] احفظ رقم الطلبية (مثل: ORD-abc123)
```

### الخطوة 2: Track Order
```
[ ] اضغط "Track Order" أو اذهب /track-order
[ ] أدخل الهاتف: 0555123456 ✅ (نفسه من الخطوة 1!)
[ ] اضغط "تحقق الآن"
```

### ✅ النتيجة المتوقعة:
```
[ ] ينقل تلقائياً إلى: /register?orderId=ORD-abc123&phone=0555123456
[ ] ترى رسالة خضراء:
    "✅ تم العثور على طلبيتك!"
    "سيتم ربط الطلبية برقم: 0555123456"
[ ] حقل الهاتف ملأ تلقائياً: 0555123456
[ ] حقل الهاتف مقفول (disabled) - نص توضيحي أسفله
```

### الخطوة 3: Register
```
[ ] ملّ البيانات:
    [ ] الاسم الأول: أحمد
    [ ] الاسم الأخير: علي
    [ ] الهاتف: 0555123456 (ملأ + locked) ✅
    [ ] كلمة المرور: Test123@
    [ ] تأكيد: Test123@
[ ] اضغط "Sign Up"
```

### ✅ النتيجة المتوقعة:
```
[ ] يتم تسجيل الدخول تلقائياً
[ ] ينقل إلى: /track-order?orderId=ORD-abc123
    (لكن هذه المرة كمستخدم مسجل!)
```

### الخطوة 4: My Orders
```
[ ] اذهب /account/orders (أو /account)
[ ] شوف "My Orders"
```

### ✅ النتيجة المتوقعة:
```
[ ] ترى الطلبية القديمة! (ORD-abc123) ✅
[ ] البيانات صحيحة:
    [ ] رقم الطلب: ORD-abc123
    [ ] الحالة: pending/confirmed
    [ ] السعر: صحيح
    [ ] المنتجات: صحيحة
    [ ] العنوان: شارع الاستقلال
    [ ] الهاتف: 0555123456
```

---

## 🔍 Debug Checks:

### في Browser Console (F12):
```javascript
// ابحث عن هذه الرسائل:

// في Track Order:
✅ "Phone verified! Redirecting to register..."

// في Register:
✅ "User created: [user-id]"
✅ "Linking guest orders and addresses to new user via API"
✅ "Guest orders linked successfully via API"
```

### في Supabase:
```sql
-- تحقق من الطلبية:
SELECT * FROM orders WHERE id = 'ORD-abc123';

-- نتيجة متوقعة:
user_id: [user-id]  -- ✅ يجب أن يكون ملأ (لا NULL)
guest_phone: 0555123456
status: pending/confirmed

-- تحقق من العنوان:
SELECT * FROM addresses WHERE user_id = '[user-id]';

-- يجب أن ترى عنوان واحد على الأقل ✅
```

---

## ❌ إذا لم تعمل:

### المشكلة 1: لا ينقل إلى Register
```
السبب: Phone لم يتطابق
الحل: تأكد من نفس الرقم بالضبط (بدون مسافات)
```

### المشكلة 2: الهاتف لم يملأ تلقائياً
```
السبب: URL parameter لم يمرر
الحل: تحقق من URL بعد Redirect
     /register?...&phone=0555123456
```

### المشكلة 3: طلبية لم تظهر بعد التسجيل
```
السبب: الربط لم يحدث
الحل: شغّل SQL Script 033 على Supabase
     (تأكد من RLS Policies)
```

### المشكلة 4: خطأ في Console
```
افتح F12 وشوف الأخطاء الفعلية
انسخ الخطأ وبحث عن السبب
```

---

## 📝 Checklist النهائي:

```
بعد كل اختبار اكتب:
[ ] ✅ Checkout يعمل
[ ] ✅ Track Order ينقل إلى Register
[ ] ✅ رسالة خضراء تظهر
[ ] ✅ الهاتف ملأ تلقائياً
[ ] ✅ حقل الهاتف مقفول
[ ] ✅ Registration يعمل
[ ] ✅ الطلبية ظهرت في My Orders
[ ] ✅ البيانات صحيحة (عنوان، منتجات، إلخ)

إذا كل شيء ✅ → النظام يعمل بشكل مثالي! 🎉
```

---

**الاختبار = 10 دقائق فقط!** ⏱️
